<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.travel.mapper.TravelMapper">
    <sql id="search">
        <if test="keyword != null">
            AND (
                district LIKE CONCAT('%', #{keyword}, '%')
                OR title LIKE CONCAT('%', #{keyword}, '%')
                OR address LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
    </sql>

    <select id="list" resultType="com.spring.travel.domain.Travel">
        SELECT
        a.no,
        a.district,
        a.title,
        a.description,
        a.address,
        a.phone,
        GROUP_CONCAT(b.filename ORDER BY b.filename SEPARATOR ',') as filename
        FROM travel a
        INNER JOIN travel_image b ON a.no = b.travel_no
        WHERE a.no > 0
        <include refid="search"/>
        GROUP BY a.no, a.district, a.title, a.description, a.address, a.phone
        ORDER BY a.no
    </select>
</mapper>